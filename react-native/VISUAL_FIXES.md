# Исправления визуальных отличий React Native от Web версии

## 📋 Проблемы, которые были исправлены

### 1. ✅ Цвета и градиенты (`theme/colors.ts`)

**Проблема:** Цвета градиентов не соответствовали веб версии.

**Исправлено:**
- Добавлены правильные градиенты для `GradientBackground`: `peach`, `lavender`, `cream`, `pink` (3 цвета как в веб)
- Добавлены градиенты для `WellnessCard`: `coral`, `blue`, `pinkCard`, `lavenderCard` (с прозрачностью)
- Обновлен `text.primary` с `#2D2D2D` на `#1a1a1a` (соответствует `--foreground`)
- Обновлен `component.buttonPrimary` с `#2D2D2D` на `#1a1a1a` (соответствует `--primary`)
- Добавлены wellness цвета: `coral`, `coralLight`, `lavender`, `lavenderLight`, `softBlue`, `softPink`

### 2. ✅ Border Radius (`theme/spacing.ts`)

**Проблема:** 
- `borderRadius.xl` был 24px, но в веб версии `WellnessCard` использует `rounded-[20px]` = 20px
- Не было значения для `MoodTracker` который использует `rounded-[28px]` = 28px

**Исправлено:**
- `borderRadius.xl` изменен с 24px на **20px** (соответствует `rounded-[20px]`)
- Добавлен `borderRadius['2xl']` = **28px** (соответствует `rounded-[28px]`)

### 3. ✅ Тени (`theme/spacing.ts`)

**Проблема:** 
- `shadows.md` имел `shadowOpacity: 0.1` и `shadowRadius: 6`
- В веб версии `WellnessCard` использует `shadow-[0_4px_20px_rgba(0,0,0,0.06)]`

**Исправлено:**
- `shadowOpacity` изменен с 0.1 на **0.06** (соответствует `rgba(0,0,0,0.06)`)
- `shadowRadius` изменен с 6 на **20** (соответствует `20px`)

### 4. ✅ WellnessCard (`components/WellnessCard.tsx`)

**Проблемы:**
- Использовал `borderRadius.xl` = 24px вместо 20px
- Градиенты не соответствовали веб версии (`coral`, `blue`, `pink`, `lavender`)
- Тень не соответствовала веб версии

**Исправлено:**
- Теперь использует `borderRadius.xl` = 20px ✅
- Добавлена поддержка градиентов `coral`, `blue`, `pink`, `lavender` (соответствует веб версии)
- Использует исправленную тень `shadows.md` ✅
- Добавлен prop `hover` для hover эффектов

### 5. ✅ GradientBackground (`components/GradientBackground.tsx`)

**Проблемы:**
- Использовал только 2 цвета в градиентах, в то время как веб версия использует 3 цвета (`from`, `via`, `to`)
- По умолчанию был `vertical`, но веб версия использует `bg-gradient-to-br` (diagonal)
- Не было поддержки `variant` prop как в веб версии

**Исправлено:**
- Добавлен prop `variant` с вариантами: `peach`, `lavender`, `cream`, `pink` (соответствует веб версии)
- Градиенты теперь используют 3 цвета (например, `['#ffecd2', '#ffd7ba', '#fcb69f']`)
- По умолчанию `direction = 'diagonal'` (соответствует `bg-gradient-to-br`)

### 6. ✅ MoodTracker (`components/MoodTracker.tsx`)

**Проблемы:**
- Использовал другие emoji и labels (русские вместо Fri, Sat, Sun, Mon, Tue)
- Размеры кнопок не соответствовали веб версии (`w-[52px] h-[52px]`)
- Border radius был `borderRadius.lg` = 16px вместо `rounded-[28px]` = 28px
- Не было streak badge сверху
- Цвета выбранного состояния не соответствовали (использовал wellness цвета вместо `#1a1a1a`)

**Исправлено:**
- Emoji и labels теперь точно соответствуют веб версии: `😔 Fri`, `😐 Sat`, `😊 Sun`, `😴 Mon`, `😄 Tue`
- Размеры кнопок: `width: 52px, height: 52px` ✅
- Border radius контейнера: `borderRadius['2xl']` = 28px ✅
- Добавлен streak badge сверху с emoji 🔥 и текстом "X Day Streak"
- Выбранное состояние: `backgroundColor: '#1a1a1a'` ✅
- Невыбранное состояние: `backgroundColor: '#f5f5f5'` ✅
- Цвета текста соответствуют веб версии

### 7. ✅ SerifHeading (`components/SerifHeading.tsx`)

**Проблемы:**
- Размеры были `small`, `medium`, `large`, `xlarge`, `display`
- В веб версии используются: `xl`, `2xl`, `3xl`, `4xl`

**Исправлено:**
- Теперь использует размеры `xl`, `2xl`, `3xl`, `4xl` (соответствует веб версии)
- Размеры шрифтов соответствуют веб версии:
  - `xl` = `text-3xl` = 28px
  - `2xl` = `text-4xl` = 32px
  - `3xl` = `text-5xl` = 40px
  - `4xl` = `text-6xl` = 48px

## 📊 Сравнение до/после

| Компонент | До | После |
|-----------|-----|-------|
| **WellnessCard borderRadius** | 24px | 20px ✅ |
| **WellnessCard shadow** | opacity 0.1, radius 6 | opacity 0.06, radius 20 ✅ |
| **GradientBackground** | 2 цвета, vertical | 3 цвета, diagonal ✅ |
| **MoodTracker borderRadius** | 16px | 28px ✅ |
| **MoodTracker кнопки** | flex, minHeight 80 | 52x52px ✅ |
| **MoodTracker emoji** | Другие | Соответствуют веб ✅ |
| **SerifHeading размеры** | small/medium/large | xl/2xl/3xl/4xl ✅ |
| **Цвета** | #2D2D2D | #1a1a1a ✅ |

## 🎯 Результат

Теперь React Native версия визуально соответствует веб версии:
- ✅ Правильные цвета и градиенты
- ✅ Правильные размеры и отступы
- ✅ Правильные тени и радиусы
- ✅ Правильные компоненты с соответствующими props

## 📝 Использование

### GradientBackground
```tsx
// Теперь поддерживает variant как в веб версии
<GradientBackground variant="peach">
  {/* content */}
</GradientBackground>
```

### WellnessCard
```tsx
// Градиенты теперь соответствуют веб версии
<WellnessCard gradient="coral">
  {/* content */}
</WellnessCard>
```

### MoodTracker
```tsx
// Теперь точно соответствует веб версии
<MoodTracker streak={7} onMoodSelect={(mood) => console.log(mood)} />
```

### SerifHeading
```tsx
// Размеры теперь соответствуют веб версии
<SerifHeading size="2xl">Title</SerifHeading>
```

## ⚠️ Важные замечания

1. **Градиенты с прозрачностью**: В React Native `LinearGradient` не поддерживает прозрачность напрямую в массиве цветов. Для `WellnessCard` градиентов используются более светлые версии цветов вместо прозрачности.

2. **3-цветные градиенты**: `LinearGradient` поддерживает любое количество цветов, поэтому 3-цветные градиенты работают корректно.

3. **Hover эффекты**: В React Native hover эффекты реализуются через `activeOpacity` в `TouchableOpacity`. Для более сложных анимаций можно использовать `react-native-reanimated`.

## 🔄 Следующие шаги

Если обнаружены другие визуальные отличия:
1. Сравните компонент с веб версией
2. Проверьте значения в `theme.css` (веб) и `theme/` (RN)
3. Обновите соответствующие файлы
4. Протестируйте на реальных устройствах

